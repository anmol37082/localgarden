import CheckoutPage from "../../features/cart/components/CheckoutPage";

export const metadata = {
  title: "Checkout | PlantBoost",
  description: "Review your cart and complete checkout locally without a backend.",
};

export default function Page() {
  return <CheckoutPage />;
}
